db._query(`FOR person in cloudseer_peoples UPDATE person WITH { global_badgecount_by_week_of_year: (FOR q in cloudseer_time_buckets FOR n in 1..53 FILTER n == q.week_number COLLECT bucket_week_of_year = n WITH count INTO length RETURN {bucket_week_of_year:bucket_week_of_year, count:length})} IN cloudseer_peoples  OPTIONS { mergeObjects: true }`).getExtra()
