db._query(`FOR person in cloudseer_peoples UPDATE person WITH { global_vpncount_by_day: (FOR n in 1..31 FOR q in cloudseer_vpn_timebuckets FILTER n == q.day COLLECT bucket_day = n WITH count INTO length RETURN {bucket_day:bucket_day, count:length})} IN cloudseer_peoples  OPTIONS { mergeObjects: true }`).getExtra()
