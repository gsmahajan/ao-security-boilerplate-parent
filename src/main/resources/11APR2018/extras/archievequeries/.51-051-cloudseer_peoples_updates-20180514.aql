db._query(`FOR person IN cloudseer_peoples UPDATE person WITH { reports_to_id: (FOR e in cloudseer_peoples FILTER e.displayName == person.reports_to RETURN { value : e._id })[0].value } INTO cloudseer_peoples`).getExtra()
