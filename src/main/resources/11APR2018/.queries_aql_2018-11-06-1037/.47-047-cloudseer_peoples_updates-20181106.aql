db._query(`FOR person IN cloudseer_peoples UPDATE person WITH { user_vpncount_by_weekday: (FOR q in cloudseer_vpn_timebuckets FOR n in 1..7 FILTER q.week == n AND UPPER(q.event.Identity) == UPPER(person.logon) COLLECT bucket_week = q.weekday WITH count INTO length RETURN {bucket_week:bucket_week, count:length})} IN cloudseer_peoples  OPTIONS { mergeObjects: true }`).getExtra()
