db._query(`FOR person IN cloudseer_peoples UPDATE person WITH { user_vpncount_by_week_of_year: (FOR q in cloudseer_vpn_timebuckets FOR n in 1..53 FILTER q.week_number == n AND UPPER(q.event.Identity) == UPPER(person.logon) COLLECT bucket_week_of_year = n WITH count INTO length RETURN {bucket_week_of_year:bucket_week_of_year, count:length})} IN cloudseer_peoples  OPTIONS { mergeObjects: true }`).getExtra()
