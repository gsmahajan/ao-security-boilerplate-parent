db._query(`FOR person IN cloudseer_peoples UPDATE person WITH { user_vpncount_by_hour: (FOR q in cloudseer_vpn_timebuckets FOR n in 0..23 FILTER n == q.hour AND UPPER(q.event.Identity) == UPPER(person.logon) COLLECT bucket_hour = n WITH count INTO length RETURN {bucket_hour:bucket_hour, count:length})} IN cloudseer_peoples  OPTIONS { mergeObjects: true }`).getExtra()
